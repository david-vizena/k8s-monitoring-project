apiVersion: apps/v1
kind: Deployment
metadata:
  name: david-vizena-app
  namespace: david-vizena
  labels:
    app: david-vizena-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: david-vizena-app
  template:
    metadata:
      labels:
        app: david-vizena-app
    spec:
      containers:
      - name: david-vizena-app
        image: davidvizena/hello-world:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
